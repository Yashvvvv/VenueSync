package com.venuesync.app.core.repository

/*
 * TODO(Yash) — M1 (Jul 26 session), YOUR HANDS per the Boundary Law:
 *  - define the interface (list + detail, Result<T, ApiError> or similar)
 *  - implementation over EventsApi with error mapping (see ApiError.kt notes)
 *  - pagination handling (Spring Page<T>, manual load-more — no Paging3)
 * Scaffold intentionally left empty; interviews ask "why" about everything in here.
 */
